const handleSupplierContractActions = (action, rowData) => {
    switch (action) {
        case 'Add Timesheet':
            console.log('Add Timesheet');
            break;
        case 'Timesheet Hours':
            console.log('Timesheet Hours');
            break;
        default:
            break;
    }
};

export default handleSupplierContractActions;